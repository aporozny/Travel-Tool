# Risk Register — Drift Discovery Engine

| ID | Risk | Likelihood | Impact | Response | Owner | Status |
|---|---|---|---|---|---|---|
| R1 | Google Geocoding API not enabled on existing key → geocoding fails | M | M | Code falls back to legacy region-string search; enable API in Google Cloud console if hit | Andre | Open |
| R2 | Google Places cost blowout once app is worldwide (every new city = fresh fetches) | M | H | Coverage threshold + 30-day cache + daily fetch budget (FETCH_DAILY_BUDGET, default 200/day, WP4.2). Billing alert in Google console still recommended | Andre | **Mitigated 2026-07-13** |
| R3 | Changing `score_breakdown` semantics breaks web/mobile UI display | L | M | Keys kept identical; only values reinterpreted; UAT on production after deploy | Claude | Mitigated |
| R4 | Deploy restarts production backend; brief outage for live users | H | L | Deploy in low-traffic window; health check + rollback via git revert + rebuild | Claude | Accepted |
| R5 | places_cache uniqueness is (external_id, source) — multi-source future will create duplicates across sources | H | M | Serve-time fuzzy dedup implemented (dedup.ts: name+150m geo, source-ranked merge) | Claude | **Mitigated 2026-07-13** |
| R6 | Single GOOGLE_PLACES_API_KEY in container env; loss of key = discovery down | L | H | Key documented in DR-RUNBOOK (location, not value); regenerate via Google console | Andre | Open |
| R7 | Postgres data loss (places_cache, member data) | L | H | Nightly `drift-db` dump exists in ~/backups (root cron). **Verified 2026-08-30**: `pg_restore --list` against the latest dump confirms a valid CUSTOM-format archive, 343 TOC entries, 52 tables with data, `pg_dump` version matching the live server. Doesn't yet cover a full restore-into-scratch-DB-and-boot test -- worth doing before actually needing this, not just before this note | Andre | **Mitigated 2026-08-30** |
| R8 | Verified-bonus cap reduces operator upsell incentive | M | L | Business decision documented; tier benefits can move to placement (badges, "featured" row) not rank distortion | Andre | Open |
| R9 | Seller-of-Travel registration exposure for Duffel flights/hotels bookings (CA/FL/WA/HI/NV register based on customer residence, not company location) | M | H | Legal review required before go-live in those states; no code-level exclusion of any state or region -- explicit Executive instruction | Andre | Open |
| R10 | `GOOGLE_PLACES_API_KEY` exposed in git history (public repo) even after removal from the current `docker-compose.yml` | H | M | **Resolved 2026-08-29** (found during Stage 13): the leaked key belonged to an old, unrestricted 33-API key in the "Openclaw" Google Cloud project (confirmed as the real production project via its Metrics history -- an initial rotation attempt had mistakenly targeted a different, unused project with zero real traffic). A new key was created in the correct project, restricted to Geocoding API + Places API + the VPS's IP address, live-tested against both APIs, and deployed. Follow-up: delete the old unrestricted key once the new one's stability is confirmed over a few days | Andre | **Mitigated 2026-08-29** |
| R11 | Voice agent had no live SIP transfer -- caller told a reviewer will follow up, not connected live | L | M | Built 2026-08-24 on a 4-agent research round (see STAGE-PLAN-7.md): no vendor exists for "call any country's police via one API" (even Life360 caps real dispatch at ~11 countries), so `connect_to_reviewer` warm-transfers to Drift's OWN human reviewer instead, via LiveKit's `WarmTransferTask` (caller never at risk of being dropped -- merge only happens once the reviewer actually answers). Deployed, container verified registering cleanly with LiveKit Cloud. Not yet live: needs an outbound SIP trunk (unclear if the unused TWILIO_* creds already in `.env` are the right account -- not assumed) and `SAFETY_REVIEWER_PHONE` (R13). Also NOT decided: whether this feature should be geography-scoped as a deliberate exception to the Duffel "never exclude a geography" instruction -- flagged to Andre, not silently resolved | Andre | Open -- code complete, outbound trunk + reviewer number + geography decision pending |
| R12 | Duffel Flights/Stays payment architecture -- changes PCI scope and possibly Seller-of-Travel exposure | M | H | Built and live-verified 2026-08-23: Payment Intent -> Balance -> Order, chosen because it's the only Duffel model that lets Drift keep a markup without running its own separate card processor (full reasoning: STAGE-PLAN-10.md WP10.6). Real test booking completed end-to-end on production, confirmation M6UT4V, confirmed in `flight_orders`. Remaining gap: Duffel's Balance payout mechanics (minimum amount, timing, any withdrawal fee) still NOT confirmed -- their payout help article 403'd on every fetch attempt, needs a direct question to Duffel before this carries real (non-test) money, since it determines how/when Drift actually gets its earned margin out | Andre | Open -- payout mechanics unconfirmed |
| R13 | `SAFETY_REVIEWER_EMAIL`/`SAFETY_REVIEWER_PHONE` unset -- voice agent escalations log but page no one | H | H | Set 2026-08-27 (real values, wired into backend + voice-worker, containers restarted and confirmed). Live-tested end-to-end: SMS via Mobile Message confirmed delivered (real message_id) -- this is the operative escalation channel. Email side tracked separately as I24 (SendGrid free-plan cap, fix deliberately deferred by Andre to launch prep) | Andre | **Mitigated 2026-08-27** -- SMS channel live and sufficient for now; email deferred (I24) |
| R14 | No Privacy Policy or Terms of Service exist while Drift collects emails, phone numbers, safety-contact info, and voice call transcripts | M | H | Draft v0.2 exists (5-lens review pass), now live at drifttravel.app/legal/privacy-terms.html with its DRAFT banner intact (Andre's explicit call 2026-08-30, given the draft itself said "not to be published as-is" and still has real gaps -- liability/indemnification, minimum age, DMCA contact, governing law all still `NEEDS LEGAL INPUT`). **2026-08-30 engineering punch-list items closed**: signup now requires an explicit `acceptedTerms` checkbox (rejected with a real error otherwise), acceptance logged to `consent_records` (versioned `terms_of_service_v0.2`, live-verified end-to-end); Trip Mode's peer-visibility now has a block affordance, not just report (see I28). **Still open, needs Andre/counsel, not code**: the counsel-facing punch list (age minimum, liability/indemnification/governing-law/dispute-resolution drafting, expanded Seller-of-Travel state list, retention periods, international-transfer mechanism), DMCA agent registration (real US Copyright Office filing), and the accessibility commitment level (business decision) | Andre | Open -- engineering punch list closed, counsel review still the blocker |
| R15 | Duffel Stays is not enabled on the Drift account -- confirmed live 2026-08-24 (`POST /stays/search` returns 403 "This feature is not enabled for your account. Please contact sales") | L | H | Contact Duffel sales to enable Stays; search/route code is already built and verified correct against the real API, just blocked on account access -- see STAGE-PLAN-11.md | Andre | Open |

Review at each stage boundary.
